class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int alt=0, max_alt=0;
       for(auto it: gain){
           alt+=it;
           max_alt=max(max_alt,alt);
           
       }
        return max_alt;
    }
};
