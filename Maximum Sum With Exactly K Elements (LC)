class Solution {
public:
    int maximizeSum(vector<int>& nums, int k) {
        int maxi=INT_MIN,sum;
        for(auto it:nums)
            maxi=max(it,maxi);
            
        
            
        
        sum=k*(k-1)/2;
        return k*maxi+sum;
        
            
    }  
    
};
